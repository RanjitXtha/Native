import { Stack, Tabs } from "expo-router";

export default function RootLayout() {
  return  <Stack screenOptions={{ headerShown: false }}>
      {/* No need to explicitly define (tabs), login, or signup here */}
    </Stack>
}
